import React from 'react';
import ReactDOM from 'react-dom'; // this library allows you to insert app components into the starter html file (id="root")
import './index.css';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));